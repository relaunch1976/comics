# 設計書の検証結果（2026-08-15）

対象: [comics-tracker-design.md](comics-tracker-design.md)

## 検証方法

2段階で実施した。

1. **アプリID取得前** — 楽天ブックス公開検索ページの実データ、公式APIドキュメント（日英）、公式アナウンス、不正アプリIDでのHTTP応答
2. **アプリID取得後** — APIテストフォームで `title=路傍のフジイ` を実行し、**実レスポンス（全8件）を取得**

§6 の同定ロジックは、この実レスポンスに対して**再検証済み**（末尾に検証済みコードを掲載）。

> **重要な前提の訂正**
> 設計書 §6 §7 の「実データ」は、APIレスポンスではなく**楽天ブックスのWebページ**から採取されたものだった。両者は内容が異なる（後述 D-4, D-5）。Webページ由来の記述は API では成立しないものがある。

---

## 🔴 A. 設計を変えないと動かない

### A-1. エンドポイントが廃止済み。`accessKey` が必須化された

楽天ウェブサービスは 2026-02-10 に大規模なAPI移行を告知し、**2026-05-14 に旧システムを停止**した。

- 全エンドポイントを `app.rakuten.co.jp` から `openapi.rakuten.co.jp` へ移行
- **既存のAPIキーは全て非推奨。アプリを再登録し `applicationId` と `applicationSecret` を再取得**
- 旧APIバージョンは提供終了

設計書 §5 のエンドポイントは旧ドメイン・旧バージョン（`20130522`）で**廃止対象**。現行は:

```
https://openapi.rakuten.co.jp/services/api/BooksBook/Search/20170404
```

実測:

| エンドポイント | 応答 |
|---|---|
| 旧 `app.rakuten.co.jp/.../20130522` | `400 {"error":"wrong_parameter",...}`（まだ応答するが公式には停止済み。依存不可） |
| 新 `openapi.rakuten.co.jp/.../20170404` | `400 {"errors":{"errorMessage":"accessKey must be present as a query parameter or in the header"}}` |

#### 【解決済み】ドメイン制限があるため、ブラウザ完結の方針は維持できる

新しいアプリ登録フォームに「**許可されたWebサイト**」という必須項目があり、

> APIリクエストは、更新されたWebサイトリストに登録されているWebサイトからのみ受け付けます。

`accessKey` は**登録ドメインに束縛される**。ソースに露出しても第三者のサイトからは使えない。

**したがって §1「サーバーサイド処理全般を却下」§2「ブラウザのみで完結させる」はそのまま維持できる。プロキシ不要。**

留保: Origin/Referer はブラウザ外からは詐称可能なので厳密な秘匿ではない。ただし無料・読み取り専用・レート制限付きという §5 の既存のリスク受容の範囲。**登録時に `*` のみを指定しないこと。**

#### 登録済みの内容

| 項目 | 値 |
|---|---|
| アプリケーション名 | コミックス既読管理 |
| アプリケーションタイプ | Webアプリケーション |
| 許可されたWebサイト | `relaunch1976.github.io` のみ |
| 予想QPS | 1 |

`localhost` `127.0.0.1` は**この欄の書式チェックに弾かれる**（「有効なWebサイト/IPリストを入力してください」）。

##### 帰結: ローカル開発から実APIは叩けない

対応方針:

- **開発中はフィクスチャ駆動**。APIテストフォームで取得した実レスポンスをJSONで保存し、それを読ませる
- API クライアントを差し替え可能な形にし、本番のみ実APIを叩く
- 実API確認が要る段階で GitHub Pages にデプロイする
- どうしても必要なら Vite の dev プロキシで `Referer` を `https://relaunch1976.github.io` にして中継（自分のドメイン・自分のキーでの開発用途。ただし判定方式次第で通らない可能性あり）

**§11 の手順は組み替えが必要。** GitHub Pages のデプロイ（元の6番）を前倒しし、リポジトリ作成とPages有効化を最初に行う。

### A-2. CORS が通る。JSONP は不要

設計書 §5 は「CORS は JSONP で回避する。検証済み」としているが、**新エンドポイントは CORS ヘッダを返す。**

無関係なオリジンから `fetch()` した結果、レスポンスタイプは `cors` でボディも読めた。**素の `fetch()` で呼べる。**

これにより次が解消する。

- JSONP のコールバック管理・タイムアウト自前実装が不要
- **HTTPステータスが読める** → `429 too_many_requests` を検知してバックオフできる（JSONPでは不可能だった）
- Service Worker のキャッシュ除外設定が不要

※ 確認したのは 400 応答時。200 応答でも同じかは実装時に再確認すること。

---

## 🟡 B. 設計書の記述が事実と異なる

### B-1. 「頃」は未発売の印ではない（§7 の記述は誤り）

設計書 §7 は `2026年09月30日頃` について「**未確定の場合「頃」が付く**」としているが、**API実データはその逆を示した**。

| 商品 | `salesDate` | 実態 |
|---|---|---|
| 路傍のフジイ（7） | `2026年09月30日` | **未発売**（予約受付中）。「頃」なし |
| 【全巻】1-6巻セット | `2026年02月27日頃` | 発売済み。「頃」あり |
| 路傍のフジイ（6） | `2026年02月27日` | 発売済み |

**「頃」の有無で刊行済み／未発売を判定してはいけない。** 出版社が正確な日を出していないという意味しかない。

さらに公式ドキュメントは設計書に無いパターンを挙げている。

> 表示例：「YYYY年」「YYYY年MM月」「YYYY年MM月DD日」
> ※発売日が確定されていない商品については、「**上旬/中旬/下旬**」や「**頃**」「**以降**」などが付加

設計書の3パターンでは足りない（**年のみ / 上旬・中旬・下旬 / 以降**が抜けている）。

#### 判定には `availability` を使う

設計書はこのフィールドに触れていないが、**これが正解**。

```
1:在庫あり  2:通常3-7日程度で発送  3:通常3-9日程度で発送
4:お取り寄せ  5:予約受付中  6:メーカーに在庫確認
```

実データで **7巻だけが `"5"`（予約受付中）**、他は全て `"1"` だった。日付パースと現在日時の比較よりはるかに堅牢。

**注意: 文字列で返る**（`"5"` であって `5` ではない）。

### B-2. ページングは必須。1作品1リクエストでは終わらない

公式仕様: `hits` 最大30、`page` 最大100。レスポンスに `count` と `pageCount` が返る。

- `路傍のフジイ` は全8件 → 1ページで済む
- **`アオのハコ` は22巻時点で全35件・2ページ** → 溢れる
- 39巻の HUNTER×HUNTER、46巻の宇宙兄弟は当然溢れる

**§9 の「1件あたり1秒程度の間隔」はページングを考慮していない。** 長期シリーズは1作品2〜4リクエスト＝2〜4秒。50作品で実質2〜3分。

`sort=-releaseDate` が使えるので「最新巻だけ分かればよい」なら1〜2ページで打ち切る設計も選べる。

### B-3. JSONP のパラメータ名は `callback`（小文字 b）

設計書 §5 は `callBack`。公式は `callback`。なお A-2 の通り JSONP 自体が不要。

---

## 🟢 C. 追加で判明した実装上の要点

### C-1. `outOfStockFlag=1` を明示すること

デフォルトは `0` ＝ **品切れ・販売終了の商品を結果に表示しない**。長期シリーズは古い巻が品切れになるため、デフォルトのままだと**巻の集合に穴が開く**。

### C-2. `booksGenreId` のデフォルト値に注意

未指定なら `001`（書籍全体）でよい。**ドキュメントのサンプルにある `001004008` は「日本の小説」ジャンル**で、これを入れるとコミックスは0件になる。

参考: 実データの `booksGenreId` は本編が `001001003009`、セット商品が `001025001`（セット本）。

### C-3. レスポンスのルートキーは `Items`（大文字 I）

`formatVersion=2` を指定すると各要素のネストは解消されるが、配列のキーは `Items` のまま。ドキュメントの表記（`items`）と異なるので注意。

`formatVersion=2` は指定推奨（`Items[0].item.title` → `Items[0].title`）。

### C-4. 型が文字列と数値で混在している

| 文字列で返る | 数値で返る |
|---|---|
| `size` `availability` `reviewAverage` `isbn` `booksGenreId` `salesDate` | `itemPrice` `postageFlag` `limitedFlag` `reviewCount` `count` `page` `hits` |

`availability === "5"` のように**文字列比較**すること。

### C-5. `elements` で返却フィールドを絞れる

`itemCaption` は非常に長い（実データで1件あたり数百文字）。使わないので除外すべき。

```
elements=title,titleKana,seriesName,publisherName,size,isbn,salesDate,availability,largeImageUrl
```

### C-6. 検索結果には無関係な作品が混ざりうる

`title=路傍のフジイ` のAPI検索は**ノイズゼロ**（全8件が該当作品）だった。ただしWebページ検索では『路上の熱量』『路傍の雫（文庫版）』が、`アオのハコ` では『僕とロボコ』が混入していた。

§7 の「誤検出への逃げ道（`excludedIsbns`）は必須」という判断は維持すべき。

### C-7. 未発売巻の表紙画像は仮画像のことがある

7巻の画像だけ `.gif`（`9784098640270.gif`）で、既刊は `.jpg`（`9784098637812_1_40.jpg`）。未発売巻は NOW PRINTING のプレースホルダの可能性が高い。表紙は最新巻ではなく**既刊の最新**から取るのが無難。

### C-8. `429` のハンドリング

公式に `429 too_many_requests` が定義されている。CORS で呼べばステータスが読めるので指数バックオフを実装すること。

---

## 🔵 D. API実データで確定した仕様

### D-1. `seriesName` は「レーベル名」（§8 の答え）

```json
"title": "路傍のフジイ（7）",
"seriesName": "ビッグ コミックス",
"publisherName": "小学館",
"size": "コミック"
```

全8件が `seriesName: "ビッグ コミックス"`。**§8 の分岐は「レーベル名が返る場合 → §6 の実装をそのまま使う」で確定。**

設計書 §3 の `label` は `seriesName` から取る。§6 の絞り込み条件1（レーベル一致）はそのまま実装できる。

§7 の「レーベル名にスペースが入る」も実データで確認（`ビッグ コミックス`）。正規化必須。

> 楽天ブックスのWebページが表示する「シリーズ名： 路傍のフジイ」は**APIの `seriesName` とは別のデータ**。混同しないこと。

### D-2. `size` は文字列 `"コミック"`

公式ドキュメントの出力欄は数値コード（`9:コミック`）を載せているが、**実レスポンスは日本語の文字列**を返す。設計書 §5 の記述が正しい。

入力パラメータとしての `size` は数値（`size=9`）。**入出力で型が違う。**

**セット商品は `size: ""`（空文字）。** つまり `size === "コミック"` の条件だけで、除外語「セット」に頼らずセット商品を落とせる。

### D-3. `title` にレーベルは含まれない

`"title": "路傍のフジイ（7）"` — Webページ表示の `路傍のフジイ（7） （ビッグ　コミックス）` は title と seriesName の連結だった。

**接頭辞除去後の残りがきれいなので、巻数パースが安定する。**

### D-4. `author` は API では揺れていない

設計書 §7 は「7巻の著者は `鍋倉夫`（スペースなし）、6巻は `鍋 倉夫`（スペースあり）」としているが、**API は全8件が `"鍋 倉夫"` で統一**されていた。揺れていたのは Web ページ側。

結論（著者を絞り込みに使わない）は妥当なので維持してよいが、**根拠として挙げられている実例は API では再現しない**。

### D-5. `titleKana` は出版社によって巻数を含む。同定には使えない

路傍のフジイでは全件が `"ロボウノフジイ"`（巻数なし）で、シリーズキーとして有望に見えたが、**追加検証で否定された**。

| 商品 | `titleKana` |
|---|---|
| アオのハコ 27 | `アオノハコ` |
| アルスラーン戦記（24） | `アルスラーンセンキ24` |

集英社は巻数を含まず、講談社は含む。**一貫性がないため同定キーには使えない。** 接頭辞方式を継続する。

---

## ✅ E. 検証済みの同定ロジック

以下を実レスポンス（路傍のフジイ 全8件）で実行し、**全件期待通りに動作することを確認済み**。

### 設計書 §6 からの変更点

1. **接頭辞抽出関数を新設**（設計書には未記載だった。実装の中で最も壊れやすい部分）
2. 絞り込み条件に `size === "コミック"` と `excludedIsbns` を追加
3. **巻数パースを「残り文字列の先頭」にアンカー**。設計書の `re.search` は任意位置にマッチするため誤爆する
4. 上限を `\d{1,4}` → `\d{1,3}` に（4桁の巻は存在しない）

```ts
const norm = (s?: string) =>
  (s ?? "").normalize("NFKC").replace(/[\s　]/g, "").toLowerCase();

const EXCLUDE = ["特装版","限定版","愛蔵版","完全版","総集編","文庫版","新装版",
                 "モノクロ版","カラー版","分冊版","セット","画集"];

/** 基準の1冊から接頭辞と巻数を取り出す。評価順が重要 */
function extractPrefix(rawTitle: string) {
  const t = rawTitle.trim();
  let m;
  // (a) 末尾の括弧付き巻数   例: 路傍のフジイ（7）
  if ((m = t.match(/^(.*?)\s*[（(]\s*(\d{1,3})\s*[）)]\s*$/)))
    return { prefix: m[1].trim(), volume: +m[2] };
  // (b) 末尾の空白区切り巻数 例: アオのハコ 18
  if ((m = t.match(/^(.*?)\s+(\d{1,3})\s*$/)))
    return { prefix: m[1].trim(), volume: +m[2] };
  // (c) 途中の空白区切り巻数 例: 悪の令嬢と十二の瞳 2 〜最強従者たちと…〜
  if ((m = t.match(/^(.*?)\s+(\d{1,3})\s+.+$/)))
    return { prefix: m[1].trim(), volume: +m[2] };
  // (d) 巻数表記なし → 1巻扱い
  return { prefix: t, volume: 1 };
}

/** 候補がシリーズに属するか判定し、巻数を返す */
function resolve(item, base) {
  if (base.excludedIsbns.includes(item.isbn))            return { ng: "除外ISBN" };
  if (item.size !== "コミック")                           return { ng: "判型違い" };
  if (norm(item.seriesName) !== norm(base.label))        return { ng: "レーベル違い" };
  if (norm(item.publisherName) !== norm(base.publisher)) return { ng: "出版社違い" };
  for (const w of EXCLUDE)
    if (norm(item.title).includes(norm(w)))              return { ng: "除外語: " + w };

  const t = norm(item.title), b = norm(base.titlePrefix);
  if (!t.startsWith(b)) return { ng: "作品名が違う" };
  const rest = t.slice(b.length);
  const m = rest.match(/^[（(]?(\d{1,3})[）)]?/);   // ← 先頭にアンカー
  if (!m) return { ng: "巻数なし" };
  return { volume: +m[1] };
}

const isUnreleased = (item) => item.availability === "5";
```

### 実行結果

```
=== 接頭辞抽出 ===
  路傍のフジイ（1）                          → "路傍のフジイ" / 1巻            [括弧(末尾)]
  アオのハコ 18                              → "アオのハコ" / 18巻            [空白(末尾)]
  HUNTER×HUNTER 39                           → "HUNTER×HUNTER" / 39巻         [空白(末尾)]
  宇宙兄弟（46）                             → "宇宙兄弟" / 46巻              [括弧(末尾)]
  悪の令嬢と十二の瞳 2 〜最強従者たちと…〜   → "悪の令嬢と十二の瞳" / 2巻     [空白(途中)]
  神の目で見た限界レベル9999の仲間達を…（23） → "神の目で…仲間達を…" / 23巻  [括弧(末尾)]
  よつばと！                                 → "よつばと！" / 1巻             [巻数なし→1]

=== シリーズ同定（基準 = 路傍のフジイ（1））===
   7巻  未発売(予約受付中)   路傍のフジイ（7）
    --  判型: ""             【全巻】 路傍のフジイ 1-6巻セット
   6巻  刊行済み             路傍のフジイ（6）
   5巻  刊行済み             路傍のフジイ（5）
   4巻  刊行済み             路傍のフジイ（4）
   3巻  刊行済み             路傍のフジイ（3）
   2巻  刊行済み             路傍のフジイ（2）
   1巻  刊行済み             路傍のフジイ（1）

  → latestVolume=6 / nextVolume=7 (2026年09月30日)

=== 誤マージの回帰テスト ===
  除外 ✓  巻数なし        路傍のフジイ 公式ガイド 2026
  除外 ✓  除外語: 特装版  路傍のフジイ（25）クリアしおり付き特装版
  除外 ✓  除外語: 画集    路傍のフジイ画集
```

**「路傍のフジイ 公式ガイド 2026」は、設計書の `re.search` 版だと `2026巻` として取り込まれる。** 先頭アンカーが必須である証拠。

---

---

## 🔴 F. 追加検証（アオのハコ / アルスラーン戦記）で判明した重大な欠陥

### F-1. 特装版しか登録されていない次巻を取りこぼす（最重要）

`アルスラーン戦記` の実データ:

| 商品 | `seriesName` | `availability` | `salesDate` |
|---|---|---|---|
| アルスラーン戦記（25）クリアしおり付き特装版 | **講談社キャラクターズA** | `5`（予約受付中） | 2026年10月08日頃 |
| アルスラーン戦記（24） | 講談社コミックス | `1` | 2026年03月09日 |

**25巻は特装版しか楽天に登録されていない。** 通常版はまだ予約が始まっていない。

そして特装版は **`seriesName` が別レーベル**（`講談社キャラクターズA`）。設計書 §6 のレーベル一致条件で落ち、除外語「特装版」にも当たる。

結果、検証済みロジックをそのまま適用すると:

```
主系列(表示用) : 1,20,21,22,23,24
latestVolume   : 24
次巻            : なし  ← 25巻を取りこぼす
```

**「もうすぐ出る巻を知らせる」というこのアプリの中核機能が、まさにその場面で動かない。** §5 が挙げる提供価値の2つのうち1つが失われる。

> なお設計書 §7 の「特装版は**同じレーベルなのでレーベル絞り込みでは落ちない**。除外語リストが必須」という記述も誤り。実データでは**別レーベル**になっており、レーベル条件だけで落ちる。落ちなさすぎるのではなく、落ちすぎるのが問題。

### F-2. 解決策: 二段構えにする（検証済み）

判定を2系統に分ける。

| 系統 | 条件 | 用途 |
|---|---|---|
| **主系列** | `size==="コミック"` かつ レーベル一致 かつ 出版社一致 かつ 除外語なし | `latestVolume`、表示（タイトル・表紙・発売日） |
| **補助系列** | `size==="コミック"` かつ **出版社一致のみ** かつ 接頭辞から巻数が取れる | 「その巻が存在する」ことの検出**だけ** |

補助系列で `latestVolume` を超える巻が見つかったら、それを `nextVolume` の候補にする。表示情報は主系列を優先する。

実行結果:

```
latestVolume    : 24
主系列だけの次巻 : なし  ← 取りこぼし
補助系列で回収   : 25巻  未発売(予約受付中)  2026年10月08日頃
                        アルスラーン戦記（25）クリアしおり付き特装版
→ nextVolume = 25 (2026年10月08日頃)
```

レーベルを緩めるので誤検出は増えるが、**`excludedIsbns` の1タップ除外（§7）がその受け皿**として既に設計されている。

### F-3. `availability` に空文字とドキュメント外の挙動がある

実測値の分布（アオのハコ＋アルスラーン戦記 60件）:

```
"1": 45件   "4": 10件   "5": 2件   "": 3件
```

- **`""`（空文字）が存在する。** ドキュメントに定義がない。販売終了・購入不可の商品と思われる
- **`"4"`（お取り寄せ）は刊行済み。** アルスラーン戦記の7〜19巻が該当。「`1` 以外は未発売」とすると全滅する

**判定は `availability === "5"` の完全一致のみ**にすること。

### F-4. `seriesName` が空文字の商品がある

`天涯無限` `蛇王再臨` `暗黒神殿`（光文社の小説版）が `seriesName: ""`。

**基準の1冊のレーベルが空だと、レーベル一致条件が `"" === ""` で通ってしまい、無関係な商品を大量に拾う。** 実装では基準レーベルが空の場合を明示的に弾くこと。

追加画面で `seriesName` が空の商品を基準に選ばせない、という対処も要る（§8 の「除外語を含む商品を選ばせない」と同じ扱い）。

### F-5. ページングの実測。1ページ目には穴がある

| 作品 | `count` | `pageCount` | 1ページ目の中身 |
|---|---|---|---|
| 路傍のフジイ | 8 | 1 | 全巻 |
| アオのハコ | 35 | 2 | 1〜27巻すべて（+セット+小説2冊）＝偶然全部入った |
| **アルスラーン戦記** | **165** | **6** | **1, 7, 8, 12〜24 のみ。2〜6, 9〜11 が無い** |

デフォルトの `sort=standard` は**巻数順でも発売日順でもない**。アオのハコの並びは `27,セット,26,25,24,23,21,22,9,4,11,10,19,…` と無秩序だった。

- **全巻を集めるなら全ページ取得が必須**。アルスラーン戦記で6リクエスト＝6秒
- **最新巻だけでよいなら `sort=-releaseDate` で1ページ目に寄せられる**

§9 の「1件あたり1秒」は後者を前提にしないと成立しない。**このアプリは `readUpTo` と `latestVolume` の差分しか見ないので、全巻を集める必要はない。** `sort=-releaseDate` で1〜2ページに絞る設計を推奨する。

### F-6. 未発売巻の画像は `noimage` プレースホルダ

`アオのハコ 27` の画像は `.../book/cabinet/noimage_01.gif`。C-7 の裏付け。

`largeImageUrl` に `noimage` を含むかで判定でき、表紙は既刊の最新巻から取ればよい。

### F-7. `salesDate` の月のみパターンを実データで確認

`汗血公路: "2013年06月"`、`アルスラーン戦記（1）: "2014年04月"`。§7 の3パターン目が実在することを確認。

### F-8. 小説版は `subTitle` に作品名＋巻数を持つ

```json
{ "title": "天涯無限", "subTitle": "アルスラーン戦記　16", "size": "新書" }
```

`title` が別名なので接頭辞一致では拾わず、`size` でも落ちるため二重に安全。ただし **`subTitle` にシリーズ情報が入るパターンがある**ことは記録しておく。

---

## 残る未検証

1. 200 応答でも CORS ヘッダが付くか
2. 品切れ巻がある長期シリーズで `outOfStockFlag` の効果
3. `sort=-releaseDate` を指定したときの実際の並び（特装版が先頭に来るか）
4. F-2 の補助系列が、他作品でどの程度の誤検出を生むか

`limitedFlag` は**特装版でも `0`** だったため、除外には使えないことが確定（検証完了）。

---

## 設計書の修正が必要な箇所

| 箇所 | 内容 |
|---|---|
| §3 | `label` の取得元は `seriesName`（D-1） |
| §3 | `salesDate` 系を構造化型に。`availability` を保持（B-1） |
| §3 | ファイル全体に `schemaVersion` と `updatedAt` を追加 |
| §5 | エンドポイントを `openapi.rakuten.co.jp/.../20170404` へ（A-1） |
| §5 | 「CORS は JSONP で回避」→ 素の `fetch()` で可（A-2） |
| §5 | `callBack` → `callback`（B-3） |
| §5 | リクエストパラメータの節を新設（`size=9` `outOfStockFlag=1` `formatVersion=2` `elements` `sort` `hits` `page`） |
| §5 | 型の混在（C-4）とルートキー `Items`（C-3）を追記 |
| §6 | 接頭辞抽出関数を追加。巻数パースを先頭アンカーに。`size` と `excludedIsbns` を条件に追加（E） |
| §6 | **主系列／補助系列の二段構えにする（F-1, F-2）。最優先** |
| §6 | 基準レーベルが空文字の場合を弾く（F-4） |
| §7 | 「特装版は同じレーベルなので落ちない」は誤り。実際は別レーベル（F-1） |
| §7 | 「頃 ＝ 未確定」を削除。判定は `availability === "5"` の完全一致（B-1, F-3） |
| §7 | `salesDate` のパターンを6種に（B-1） |
| §7 | 著者名の揺れの実例は API では再現しない（D-4）。結論は維持 |
| §8 | 検証完了。`seriesName` はレーベル名（D-1） |
| §9 | `sort=-releaseDate` で1〜2ページに絞る方針を明記（F-5）。全ページ取得は最大6リクエスト |
| §4 | 追加画面で `seriesName` が空、または除外語を含む商品を基準に選ばせない（F-4） |
| §11 | GitHub Pages デプロイを前倒し。フィクスチャ駆動開発に（A-1） |

### 設計書のレビューで挙げた未解決の指摘（API検証とは独立）

- `readUpTo` の初期値・上限・取り消し手段が未定義
- 一覧の4セクションが排他でない（優先順位の明記が必要）
- OneDrive の last-write-wins とバックグラウンド更新の組み合わせで `readUpTo` が消えうる
- 追加画面で除外語を含む商品を基準に選ばせない（選ぶと以後どの巻ともマッチしない）
- 同一巻数が複数残った場合の選択基準（「ISBNが最小」は根拠が薄い。発売日が最も早いものを推奨）
