# コミックス既読管理アプリ 設計書（実装引き継ぎ用）

作成日: 2026-08-15
最終更新: 2026-08-15（楽天API実データによる検証を反映）
状態: 設計確定・実装未着手

検証の根拠と経緯は [comics-tracker-verification.md](comics-tracker-verification.md) を参照。
本書に書かれたAPIの挙動は、すべて実レスポンスで確認済み。

---

## 1. このアプリは何か

追っている漫画シリーズについて、**「自分がどこまで読んだか」と「刊行済みの最新巻」の差**を可視化するアプリ。

主目的は **新刊の取りこぼし防止**。既読管理はそのための手段であって、読書記録アプリではない。

解きたい具体的な問題:
> 「3巻が出ているのに、自分は1巻までしか読んでいない」ことに気づけない

### やらないこと（意図的に捨てた機能）

実装時にこれらを「あった方が親切だから」と足さないこと。すべて検討の上で却下している。

| 機能 | 却下理由 |
|---|---|
| プッシュ通知・メール通知 | 実装しない。OS標準のリマインダーで代替する |
| 所持巻・積読の管理 | 記録タイミングが増えて続かなくなる |
| 巻ごとの既読フラグ | 「読んだ最終巻」の数値1つで足りる |
| 電子書籍の配信日 | 紙の発売日のみを基準にする |
| 感想・レビュー・評価 | 目的外 |
| 並べ替え・タブ・フィルタUI | セクション分けで足りる |
| シリーズの全巻リスト表示 | 差分だけ分かればよい。API負荷も増える |
| バーコードスキャン | v1では見送り（後から足せる） |
| サーバーサイド処理全般 | ブラウザのみで完結させる |

一度 GitHub Actions による日次バッチ + 2リポジトリ + アクセストークンという構成を設計したが、**複雑すぎるとして全面的に破棄した**。通知を実装しないと決めた時点でバッチの存在意義が消えていたため。シンプルさはこのプロジェクトの明示的な要求。

---

## 2. 構成

```
[ 端末内のデータ ] ←→ [ アプリ（静的ページ） ] ←→ [ 楽天ブックス書籍検索API ]
        ↕
   [ OneDrive（同期） ]
```

- ブラウザで開く静的ページ 1つ。ホスティングは GitHub Pages
  - リポジトリ: `relaunch1976/comics`
  - 公開URL: `https://relaunch1976.github.io/comics/`
  - **プロジェクトサイトなのでサブパス配信になる。** Vite の `base` を `'/comics/'` にすること（PWA のマニフェストと Service Worker のスコープも同じ）
- データは端末内の **localStorage** に保持（数十件・数KB。IndexedDB は不要）
- PC・スマホ間の同期は **OneDrive**（個人の Microsoft アカウント）
  - Microsoft Graph API + MSAL.js（SPA / PKCE、クライアントシークレットなし）
  - スコープは `Files.ReadWrite.AppFolder`（アプリ専用フォルダのみアクセス可能）
  - Azure でのアプリ登録が必要。**サポートするアカウントの種類は「個人の Microsoft アカウントのみ」**
  - リダイレクトURIは本番とローカル開発の2つを登録する
  - **会社の Microsoft アカウントは使わない**
- 楽天 API はアプリから **素の `fetch()` で直接呼ぶ**（CORS が通る。JSONP は不要）

技術スタックは既存の日記アプリと揃える（Vite + TypeScript、依存は最小限、PWA）。

### 楽天APIの認証とドメイン制限

楽天ウェブサービスは 2026-05-14 に旧システムを停止済み。現行の認証は `applicationId` + `accessKey` の2点セットで、**両方が必須**。

`accessKey` はビルド成果物に含まれるため隠せないが、アプリ登録時の「**許可されたWebサイト**」により**登録ドメインからのリクエストしか受け付けない**。したがってソースに露出しても第三者のサイトからは使えず、**ブラウザのみで完結させる方針は維持できる**。

登録済みの内容:

| 項目 | 値 |
|---|---|
| アプリケーションタイプ | Webアプリケーション |
| 許可されたWebサイト | `relaunch1976.github.io` のみ |
| 予想QPS | 1 |

`accessKey` はリポジトリに直接書かず、GitHub Actions のシークレットからビルド時に注入する。git履歴に残さず、ローテーションも楽になる。

---

## 3. データモデル

```ts
type SalesDate = {
  raw: string;                    // "2026年09月30日頃" APIの生値
  date: string;                   // "2026-09-30"       比較・表示用のISO
  precision: "year" | "month" | "day";
  tentative: boolean;             // 「頃」「上旬/中旬/下旬」「以降」が付いていた
};

type Series = {
  id: string;
  title: string;          // 表示名（基準巻のtitleから巻数を除いたもの）
  titlePrefix: string;    // 巻数判定用の接頭辞（正規化前の生の文字列）
  label: string;          // レーベル名（APIの seriesName）例: ビッグ コミックス
  publisher: string;      // APIの publisherName
  baseIsbn: string;       // 基準に選んだ1冊。誤検出調査や接頭辞の再抽出に使う
  coverUrl?: string;      // 既刊の最新巻の表紙（未発売巻は仮画像なので使わない）

  readUpTo: number;       // 読んだ最終巻。これが既読管理のすべて
  latestVolume: number;   // 刊行済みの最新巻
  latestSalesDate?: SalesDate;

  latestIsbn?: string;    // latestVolume を出した商品。「この本は違う」で除外するのに要る
  nextVolume?: number;    // 未発売の予約巻（あれば）
  nextSalesDate?: SalesDate;
  nextIsbn?: string;

  isCompleted: boolean;     // 完結マーク（手動）
  excludedIsbns: string[];  // 誤検出した商品の除外リスト
  lastCheckedAt: string;    // ISO 8601
};

type Store = {
  schemaVersion: 1;
  updatedAt: string;      // ISO 8601
  series: Series[];
};
```

### 同期するもの / しないもの

**`Series` のうち OneDrive に同期するのは、ユーザーが入力したものだけ。** API由来のフィールドは何度でも取り直せる派生データなので、同期しない。

| 種別 | フィールド |
|---|---|
| **同期する**（ユーザーの意図） | `id` `title` `titlePrefix` `label` `publisher` `baseIsbn` `readUpTo` `isCompleted` `excludedIsbns` |
| **同期しない**（端末内のキャッシュ） | `latestVolume` `latestSalesDate` `latestIsbn` `nextVolume` `nextSalesDate` `nextIsbn` `coverUrl` `lastCheckedAt` |

この分離が §9 の前提。API由来のフィールドまで同期すると、バックグラウンド更新がファイル全体を書き戻すことになり、他端末で進めた `readUpTo` を巻き戻す事故が起きる。

`readUpTo` は数値1つ。「1巻と3巻は読んだが2巻が抜けている」は表現できないが、それで良いと判断済み。将来的に巻ごとフラグへ移行する場合も `1..readUpTo` を true に展開するだけで済むため、移行コストはほぼゼロ。

`schemaVersion` は OneDrive 同期と将来の移行のために最初から持つこと。後から足すのは難しい。

---

## 4. 画面

画面は2つだけ。**一覧**と**追加**。

### 一覧

作品を4つのセクションに分けて、この順に上から並べる。並べ替え機能は作らない。

1. **未読の巻がある** — `readUpTo < latestVolume`
2. **次巻の発売日が判明している** — `nextVolume` あり
3. **次巻の予定なし** — 上記以外
4. **完結** — `isCompleted === true`

セクションは排他ではないので、**判定は if / else if で上から順に**行う。ただし**表示順と判定順は異なる**ので注意すること。

```
判定順: 未読 → 完結 → 次巻判明 → 予定なし
表示順: 未読 → 次巻判明 → 予定なし → 完結
```

完結作品は `nextVolume` を持たないため、判定で「予定なし」より後ろに置くと**すべて「予定なし」に吸い込まれて「完結」セクションが永久に空になる**。

完結作品でも未読が残っていればセクション1に出る。これは意図した挙動で、「読んだ最終巻と刊行済み最新巻の差を可視化する」という目的に合っている。

各行に「◯巻まで読んだ / 最新は◯巻」を表示する。

- **`+` をタップ** → `readUpTo` を1つ進める。上限は `latestVolume`
- **`+` を長押し** → 数値を直接入力。「17巻まで一気に読んだ」場合と、誤タップの取り消しの両方をこれで賄う

完結作品は非表示にせず、セクションとして分けて残す（読み返しや積読の確認のため）。

### 追加

タイトルを入力 → 楽天 API を検索 → 表紙付きの候補一覧 → **自分が持っている1冊をタップ**。

その1冊から `titlePrefix` `label` `publisher` を機械的に抽出して `Series` を作る。`readUpTo` の初期値は**選んだ巻の巻数**とする（「持っている＝読んだ」とみなす。ズレていれば長押しで直せる）。

**UIに「ISBN」という語を出さないこと。** ユーザーから見た操作は「検索して選ぶ」だけ。

#### 基準に選ばせてはいけない商品

次のいずれかに当たる商品は、候補一覧でグレーアウトするか非表示にする。基準にすると以後どの巻ともマッチしなくなる。

- `size !== "コミック"`（小説版・画集・セット商品）
- `seriesName` が空文字（レーベル一致条件が空同士で通ってしまい、無関係な商品を大量に拾う）
- タイトルに除外語（特装版・限定版など）を含む

---

## 5. 楽天ブックス書籍検索API

### エンドポイント

```
https://openapi.rakuten.co.jp/services/api/BooksBook/Search/20170404
```

**旧ドメイン `app.rakuten.co.jp` と旧バージョン `20130522` は 2026-05-14 に廃止済み。使わないこと。**

- `applicationId` と `accessKey` の両方が必須
- **CORS が通るので素の `fetch()` で呼べる。JSONP は不要**（JSONPを使う場合のパラメータ名は `callBack` ではなく `callback`）
- レート制限あり。**1リクエストあたり1秒程度の間隔を空けること**
- `429 too_many_requests` が定義されている。`fetch()` ならステータスが読めるので指数バックオフを実装する

### リクエストパラメータ

| パラメータ | 値 | 理由 |
|---|---|---|
| `title` | 検索語 | 部分一致 |
| `size` | `9` | コミックスのみ。小説・画集・セット商品が最初から来ない |
| `outOfStockFlag` | `1` | **必須**。デフォルトの `0` だと品切れの巻が結果から消える |
| `sort` | `-releaseDate` | 発売日の新しい順。最新巻を1ページ目に寄せる |
| `hits` | `30` | 最大値 |
| `page` | `1`〜 | 最大100 |
| `formatVersion` | `2` | レスポンスのネストが浅くなる |
| `elements` | 下記 | `itemCaption` が数百文字あるので落とす |
| `booksGenreId` | **指定しない** | デフォルトの `001`（書籍全体）でよい |

```
elements=title,seriesName,publisherName,size,isbn,salesDate,availability,largeImageUrl
```

> **`booksGenreId` の注意**: 公式ドキュメントのサンプルにある `001004008` は「日本の小説」ジャンル。コピーして使うとコミックスが0件になる。

### レスポンスの読み方

- **配列のキーは `Items`（大文字 I）。** `formatVersion=2` を指定してもここは変わらない
- 総件数は `count`、総ページ数は `pageCount`（最大6ページ程度になる作品がある）
- **型が文字列と数値で混在している**

| 文字列で返る | 数値で返る |
|---|---|
| `size` `availability` `isbn` `salesDate` `booksGenreId` `reviewAverage` | `itemPrice` `postageFlag` `limitedFlag` `reviewCount` `count` `page` `hits` |

### 使うレスポンス項目

| 項目 | 内容 | 備考 |
|---|---|---|
| `title` | 商品タイトル | レーベルは**含まれない**。巻数がこの中に埋まっている |
| `seriesName` | **レーベル名** | 例: `ビッグ コミックス`。作品名ではない。空のことがある |
| `publisherName` | 出版社 | 例: `小学館` |
| `size` | 判型 | **文字列**。コミックスは `"コミック"`。セット商品は `""` |
| `isbn` | ISBN | 除外リストのキーに使う |
| `salesDate` | 発売日 | 表記が不定。§7 参照 |
| `availability` | 在庫状況 | **文字列**。`"5"` が予約受付中＝未発売。§7 参照 |
| `largeImageUrl` | 表紙画像 | 未発売巻は `noimage_01.gif` の仮画像 |
| `author` | 著者名 | **絞り込みに使ってはいけない。§7参照**。表示用のみ |

### 取得の方針

**このアプリは `readUpTo` と `latestVolume` の差分しか見ないので、全巻を集める必要はない。**

`sort=-releaseDate` で**1ページ目、多くて2ページ目まで**取得すれば、最新巻と予約巻は必ず含まれる。全ページ取得は不要（アルスラーン戦記は165件6ページある）。

### 発売日データの限界（重要）

楽天ブックスに次巻が載るのは**予約受付が始まってから**で、これは概ね発売の1〜2ヶ月前。
**「半年後に出る次巻」を知ることは原理的にできない。**

このアプリが提供できるのは次の2つだけであり、長期の予定表にはならない。

- 出たのに気づいていない巻を知らせる
- もうすぐ出る巻を知らせる

結果として「次巻の予定なし」セクションには大量の作品が滞留する。これは仕様。

---

## 6. シリーズ同定と巻数パース

### 方針: 二段構え

**判定を2系統に分ける。これは必須。1系統では次巻を取りこぼす（理由は §7 参照）。**

| 系統 | 条件 | 用途 |
|---|---|---|
| **主系列** | `size==="コミック"` かつ **レーベル一致** かつ 出版社一致 かつ 除外語なし | `latestVolume`、表示（タイトル・表紙・発売日） |
| **補助系列** | `size==="コミック"` かつ **出版社一致のみ** かつ 接頭辞から巻数が取れる | 「その巻が存在する」ことの検出**だけ** |

補助系列で `latestVolume` を超える巻が見つかったら、それを `nextVolume` の候補にする。表示情報は必ず主系列を優先する。

レーベル条件を外す分だけ誤検出が増えるが、`excludedIsbns` の1タップ除外（§7）がその受け皿になる。

### 検証済みの実装

以下は楽天APIの**実レスポンス**（路傍のフジイ / アオのハコ / アルスラーン戦記）に対して動作確認済み。

```ts
const norm = (s?: string) =>
  (s ?? "").normalize("NFKC").replace(/[\s　]/g, "").toLowerCase();

const EXCLUDE = ["特装版","限定版","愛蔵版","完全版","総集編","文庫版","新装版",
                 "モノクロ版","カラー版","分冊版","セット","画集"];

/**
 * 基準の1冊から接頭辞と巻数を取り出す。
 * 評価順が重要。括弧付きを先に見ないと、タイトル本文の数字を巻数と誤認する。
 */
function extractPrefix(rawTitle: string) {
  const t = rawTitle.trim();
  let m;
  // (a) 末尾の括弧付き巻数   例: 路傍のフジイ（7）
  if ((m = t.match(/^(.*?)\s*[（(]\s*(\d{1,3})\s*[）)]\s*$/)))
    return { prefix: m[1].trim(), volume: +m[2] };
  // (b) 末尾の空白区切り巻数 例: アオのハコ 18
  if ((m = t.match(/^(.*?)\s+(\d{1,3})\s*$/)))
    return { prefix: m[1].trim(), volume: +m[2] };
  // (c) 途中の空白区切り巻数 例: 悪の令嬢と十二の瞳 2 〜最強従者たちと…〜
  if ((m = t.match(/^(.*?)\s+(\d{1,3})\s+.+$/)))
    return { prefix: m[1].trim(), volume: +m[2] };
  // (d) 巻数表記なし → 1巻扱い
  return { prefix: t, volume: 1 };
}

/** 接頭辞を取り除いた残りの「先頭」から巻数を取る */
function volumeOf(title: string, prefix: string): number | null {
  const t = norm(title), b = norm(prefix);
  if (!t.startsWith(b)) return null;
  const m = t.slice(b.length).match(/^[（(]?(\d{1,3})[）)]?/);
  return m ? +m[1] : null;
}

/** 主系列: 表示と latestVolume に使う。厳格 */
function isPrimary(item: Item, base: Series): boolean {
  if (base.excludedIsbns.includes(item.isbn)) return false;
  if (item.size !== "コミック") return false;
  if (!base.label) return false;                    // 基準レーベルが空なら成立しない
  if (norm(item.seriesName) !== norm(base.label)) return false;
  if (norm(item.publisherName) !== norm(base.publisher)) return false;
  for (const w of EXCLUDE)
    if (norm(item.title).includes(norm(w))) return false;
  return true;
}

/** 補助系列: 「その巻が存在する」ことの検出だけに使う。緩い */
function isSecondary(item: Item, base: Series): boolean {
  if (base.excludedIsbns.includes(item.isbn)) return false;
  if (item.size !== "コミック") return false;
  if (norm(item.publisherName) !== norm(base.publisher)) return false;
  return volumeOf(item.title, base.titlePrefix) !== null;
}

/** 未発売判定。完全一致であること（§7 参照） */
const isUnreleased = (item: Item) => item.availability === "5";
```

### 集計

```
1. 主系列を集め、未発売でないものの最大巻数を latestVolume とする
2. 補助系列のうち latestVolume を超える巻を集める
3. そのうち isUnreleased なものの最小巻を nextVolume とする
4. 表紙は主系列の latestVolume の largeImageUrl を使う（未発売巻の仮画像を避ける）
```

### 巻数の表記パターン

実データで確認されたもの。すべて対応が必要。

- 括弧付き: `路傍のフジイ（7）` `宇宙兄弟（46）` `アルスラーン戦記（24）`
- スペース区切り: `アオのハコ 18` `HUNTER×HUNTER 39`
- スペース区切りで**巻数がタイトルの途中**: `悪の令嬢と十二の瞳 2 〜最強従者たちと…〜`

**括弧のパターンを先に評価すること。** タイトル本文に数字を含む作品（例:『…レベル9999の仲間達を…（23）』）で、スペース区切りを先に見ると誤認する。

**巻数の抽出は、接頭辞を除いた残りの「先頭」にアンカーすること。** 任意位置を検索すると『路傍のフジイ 公式ガイド 2026』を **2026巻**として取り込む。上限を3桁にしているのも同じ理由。

---

## 7. 実装時の落とし穴（すべて実データで確認済み）

### 特装版しか登録されていない次巻がある（最重要）

『アルスラーン戦記』の実データ:

| 商品 | `seriesName` | `availability` |
|---|---|---|
| アルスラーン戦記（25）クリアしおり付き特装版 | **講談社キャラクターズA** | `5`（予約受付中） |
| アルスラーン戦記（24） | 講談社コミックス | `1` |

**25巻は特装版しか楽天に登録されていない。** 通常版はまだ予約が始まっていない。

そして**特装版は別レーベル**になっている。レーベル一致条件で落ち、除外語「特装版」にも当たるため、単純な絞り込みだと **25巻の存在自体を見逃す**。「もうすぐ出る巻を知らせる」という中核機能が、まさに必要な場面で動かなくなる。

**§6 の二段構えはこれを回避するためのもの。** 省略しないこと。

### 「頃」は未発売の印ではない

| 商品 | `salesDate` | 実態 |
|---|---|---|
| 路傍のフジイ（7） | `2026年09月30日` | **未発売**。「頃」なし |
| 【全巻】1-6巻セット | `2026年02月27日頃` | 発売済み。「頃」あり |

**「頃」は出版社が正確な日を出していないという意味でしかない。** 刊行済み／未発売の判定に使ってはいけない。

### 未発売の判定は `availability === "5"` の完全一致

実測値の分布（60件）:

```
"1": 45件（在庫あり）   "4": 10件（お取り寄せ）   "5": 2件（予約受付中）   "": 3件
```

- **`"4"`（お取り寄せ）は刊行済み。** アルスラーン戦記の7〜19巻が該当する。「`1` 以外は未発売」とすると全滅する
- **`""`（空文字）が存在する。** ドキュメントに定義がない。販売終了・購入不可の商品と思われる

### 発売日の表記が不定

公式仕様は `YYYY年` / `YYYY年MM月` / `YYYY年MM月DD日` の3形式に、発売日が確定していない場合は **「上旬/中旬/下旬」「頃」「以降」**などが付加される。

実データで確認したもの:

```
2026年09月30日      日まで確定
2026年02月27日頃    「頃」付き（発売済み）
2013年06月          月まで
```

パーサはこれらをすべて受け入れ、`SalesDate` に正規化すること。月までの場合は月末として扱う。

### レーベル名にスペースが入る

`ビッグ コミックス`（小学館）、`講談社コミックス`（講談社）など。比較時は必ず空白を除去して正規化すること。

### `seriesName` が空の商品がある

光文社の小説版などで `seriesName: ""` が実在する。**基準のレーベルが空だと `"" === ""` でレーベル一致条件が通ってしまい、無関係な商品を大量に拾う。** `isPrimary` で明示的に弾くこと（§6 のコードは対応済み）。追加画面でも基準に選ばせない（§4）。

### セット商品は `size` で落ちる

『【全巻】路傍のフジイ 1-6巻セット』の `size` は空文字。`size === "コミック"` の条件だけで落ちるため、除外語「セット」に頼る必要はない（保険として残しておく）。

### `limitedFlag` は特装版の除外に使えない

特装版でも `0` だった。判定材料にならない。

### 著者名で絞り込んではいけない

APIの `author` は今回の実データでは揺れていなかった（Webページ側では揺れていた）が、**絞り込みに使う必要がない**。同一シリーズでも表記が変わる可能性があり、変わったときに失うのは「一番知りたい未発売の巻」になりやすい。表示用にとどめること。

### 未発売巻の表紙は仮画像

`アオのハコ 27` の `largeImageUrl` は `.../book/cabinet/noimage_01.gif` だった。`noimage` を含むかで判定できる。表紙は既刊の最新巻から取ること。

### 同じ巻数の商品が複数残った場合

除外語をすり抜けて同一巻数の商品が複数残ることがある。**発売日が最も早いものを選ぶ**（通常版が先に出る）。残りは無視する。

### 誤検出への逃げ道を必ず用意する

どれだけ絞り込んでも誤って拾う商品は必ず出る。特に §6 の補助系列はレーベル条件を外しているので、誤検出は前提。

**「この本は違う」と1タップで除外できるボタンを実装すること**（`excludedIsbns` に追加）。これは必須機能であってオプションではない。

---

## 8. 更新タイミング

アプリを開いたときに、**前回チェックから1日以上経過した作品だけ**を裏で順次更新する。

- 画面は既存データで即座に表示し、更新できたものから差し替える（ユーザーを待たせない）
- **1作品あたり1〜2リクエスト**（`sort=-releaseDate` で1ページ目、必要なら2ページ目まで）
- 1リクエストあたり1秒程度の間隔を空ける（レート制限対策）
- 完結マークの付いた作品は問い合わせ対象から除外する
- `429` が返ったら指数バックオフ。その作品の `lastCheckedAt` は更新しない
- **他端末で追加された作品は `lastCheckedAt` を持たない**（§9 の通りAPI由来のデータは同期しないため）。この場合は必ず更新対象にする。初回だけ「最新は◯巻」が空欄になるが、数秒で埋まる

全件更新や、手動更新ボタンのみの方式は却下済み。

---

## 9. OneDrive 同期

**目的は PC とスマホでのデータ共有。** これが唯一の存在理由であり、他の用途（バックアップ等）は副産物として扱う。

ブラウザのみ・サーバーなしで2端末を自動同期する手段は、ユーザーが既に持っているクラウドストレージを OAuth で使う以外にない。`Files.ReadWrite.AppFolder` スコープの OneDrive がその中で最も筋が良い。GitHub の PAT はドメイン制限が効かない生のシークレットになるため使わない。

データ量は数十件・数KB。

### 同期するのはユーザーが入力したものだけ

**§3 の分離が前提。** API由来のフィールドは同期しない。

これにより、**§8 のバックグラウンド更新は OneDrive に一切書き込まない**。書き込みが起きるのは次の3つだけになる。

- `+` を押して `readUpTo` が変わったとき
- 作品を追加・削除したとき
- 「この本は違う」で `excludedIsbns` に追加したとき

いずれもユーザーの明示的な操作なので、**両端末で同時に起きることは実質ない。**

> API由来のフィールドまで同期すると、次の事故が起きる。設計の初期案はこれを踏んでいた。
>
> PCでアプリを開く（読込後、裏で30秒かけて更新中）
> → その間にスマホで `+` をタップして書き込み
> → PCのバックグラウンド更新が完了し、**古いスナップショットで全体を上書き**
> → `readUpTo` の更新が消える

### マージ規則

それでも取りこぼさないよう、書き込み前にリモートを読み直し、`id` 単位でマージする。

| フィールド | 規則 |
|---|---|
| `readUpTo` | **大きい方**を採る |
| `isCompleted` | `true` が勝つ |
| `excludedIsbns` | 和集合 |
| 片方にしか無い `Series` | そのまま残す（削除は明示的な削除操作のときだけ） |

すべて可換なので、**衝突検出もリトライも要らない。** 順序が入れ替わっても結果は同じになる。

書き込みは**数秒デバウンス**すること（`+` の連打対策）。

---

## 10. 開発時の制約

アプリ登録の「許可されたWebサイト」に `relaunch1976.github.io` しか登録できていない（`localhost` も `127.0.0.1` も書式チェックに弾かれた）。

**したがってローカル開発中は実APIを叩けない。**

- 開発は**フィクスチャ駆動**で行う。APIテストフォームで取得した実レスポンスをJSONで保存し、それを読ませる
- API クライアントは差し替え可能な形にし、本番のみ実APIを叩く
- 実APIの確認が要る段階で GitHub Pages にデプロイする

```
src/api/client.ts        本番: fetch で楽天API
src/api/fixtures/*.json  テストフォームで取得した実レスポンス
```

フィクスチャは §6 のロジックのユニットテストにもそのまま使う。**この設計書の価値の大半は実データでの検証結果にあるので、それをテストとしてコードに固定すること。**

---

## 11. 参考: 既存の類似サービス

「ベルアラート」（alert.shop-bell.com）が、シリーズ登録による新刊発売日のメール通知を提供している。ただし**既読巻の管理機能はない**。本アプリの差別化点は「自分がどこまで読んだか」との差分を出す部分にある。

---

## 12. 実装の進め方

1. **リポジトリ作成と GitHub Pages の有効化**（空ページでよい）
   実APIの動作確認がここに依存するので最初に行う
2. 楽天APIのフィクスチャを揃える（路傍のフジイ / アオのハコ / アルスラーン戦記の3件は取得済み）
3. §6 のロジックを TypeScript で実装し、フィクスチャでユニットテスト
4. `SalesDate` のパーサを実装しテスト（§7 の全パターン）
5. 一覧画面と追加画面（データはローカルのみ、APIはフィクスチャ）
6. APIクライアント（`fetch`、レート制限、ページング、バックオフ）を実装し、**GitHub Pages にデプロイして実APIで確認**
7. OneDrive 同期（§9 のマージ規則を含む）
8. PWA 化

OneDrive 同期は後回しでよい。それまでは端末1台で完結させ、動くものを先に手に入れることを優先する。
